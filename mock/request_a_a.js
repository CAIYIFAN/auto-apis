const path = '/request/a/a'

const method = 'GET'

const description = ''

const query = {

}

const response = {
  ret: '0',
  msg: 'success'
}

module.exports = {
  path,
  method,
  description,
  query,
  response,
}
