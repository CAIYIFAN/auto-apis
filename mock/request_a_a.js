const path = '/request/a/a'

const method = 'GET'

const description = ''

const autoTs = true

const query = {

}

const response = {
  ret: '0',
  msg: 'success',
  data: [{id: 'name'}]
}

module.exports = {
  path,
  method,
  description,
  query,
  response,
  autoTs
}
