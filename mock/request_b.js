const path = '/request/b'

const method = 'GET'

const description = ''

const query = {
  ret: '0',
  msg: 'success'
}

const response = {
  ret: '0',
  msg: 'success'
}

module.exports = {
  path,
  method,
  description,
  query,
  response,
}
